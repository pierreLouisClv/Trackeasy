package com.trackeasy.app.UI;

import javax.swing.*;
import java.awt.*;

public class DrivingView extends JPanel {

    // TO DO : Voir google Docs dans la partie à faire
    public DrivingView() {
        setLayout(new BorderLayout());
        JLabel label = new JLabel("Driver View", SwingConstants.CENTER);
        add(label, BorderLayout.CENTER);
    }
}
